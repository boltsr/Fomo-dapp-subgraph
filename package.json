{
  "name": "fomo",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --access-token 7515be4496e79a1d68cf7659b0971edb --debug --node https://api.studio.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ boltsr/Fomo-dapp-subgraph",
    "create-local": "graph create --node http://localhost:8020/ fomo",
    "remove-local": "graph remove --node http://localhost:8020/ fomo",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fomo"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
